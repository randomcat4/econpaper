"""Vendored backends bundled with skill4econ.

These exist so the package is self-contained on a fresh machine: no
external D:\\ paths required. Each backend records its provenance so the
manifest can reproduce the same result with the original upstream source.
"""
